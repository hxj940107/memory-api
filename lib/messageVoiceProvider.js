// Intentionally empty until XiaoC's permanent voice and provider are chosen.
// The adapter will expose one function that accepts message text and returns:
// { buffer, mime_type, extension, duration_seconds, provider, voice_id, voice_version }.
export function getMessageVoiceSynthesizer() {
  return null
}

